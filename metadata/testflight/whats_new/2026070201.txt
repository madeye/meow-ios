Build 2026070201 (1.3.2)

• New Utility tab: Traffic, Logs, and the new DNS panel now live behind one hub, freeing up the tab bar.
• DNS panel: inspect the engine's cached lookups live — domain, resolved IPs, upstream server, and TTL — with server-side search.
• Traffic chart and log history now survive navigating away and back within the Utility hub.
• Engine updated to meow-rs 0.16.0 (adds the DNS results API and dependency security fixes).
