Fixes proxy routing rules for connections that use fake-IP DNS. Domain-based rules (e.g. routing specific sites direct vs. through a proxy) are now applied correctly to TCP connections instead of all falling through to the default proxy, and connections no longer occasionally fail to a placeholder address. Also improves overall connection reliability after the latest engine update.
