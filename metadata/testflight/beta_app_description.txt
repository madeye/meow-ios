meow is a native iOS proxy client built on meow-rs — Clash-style YAML subscriptions, rule-based routing, DoH, and a Liquid Glass UI on iOS 26. Traffic is carried by a NetworkExtension packet tunnel running entirely on-device; meow does not provide servers.

This public beta is for testers who want to try the iOS port and report issues. Bring your own Clash YAML subscription. Please report crashes, UI glitches, and routing mismatches via the TestFlight feedback button or on GitHub.

Known limitation: non-DNS UDP traffic is not yet forwarded — QUIC/HTTP3 falls back to TCP HTTP/2.

The app does not collect analytics, subscription URLs, proxy credentials, configuration contents, DNS queries, or tunneled traffic.

GitHub: https://github.com/madeye/meow-ios
