// Imported by the app target via SWIFT_OBJC_BRIDGING_HEADER so the unified
// mihomo-rust C surface (subscription conversion + YAML validation) is
// visible to Swift. The app target links against the same
// `MihomoCore.xcframework` as the extension but never touches the
// engine/tun2socks lifecycle — it only calls the pure-function subset.
#pragma once

#import "mihomo_core.h"
